---
import Section from './Section.astro';
import Tag from './Tag.astro';
import { skills } from '../data/skills';
---
<Section title="Skills">
  <div class="flex flex-wrap gap-2">
    {skills.map((s) => <Tag variant="interactive">{s}</Tag>)}
  </div>
</Section>